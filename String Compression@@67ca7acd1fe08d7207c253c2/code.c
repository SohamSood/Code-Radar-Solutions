// Your code here...
int compressString(int *str,int *a) {
    for(int i= 0;i<(str(len));i++) {
        int count = 0;
        int var = str[i];
        for(int j =0;j<str(len);j++) {
            if(str[i] == str[j]) {
                count++;
            }
        }
    }
}